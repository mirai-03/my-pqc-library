# My Quantum Vault (PQC Library)
This is a post-quantum cryptographic library using NIST-approved Kyber (ML-KEM).

## Requirements
- liboqs (Open Quantum Safe) installed in /usr/local/
- OpenSSL (libssl-dev)

## How to Build
1. Clone the repo.
2. Run `make`.
3. Run `./pqc_tester`.
